{#
/**
 * @file
 * Theme override to display a region.
 *
 * Available variables:
 * - content: The content for this region, typically blocks.
 * - attributes: HTML attributes for the region <div>.
 * - region: The name of the region variable as defined in the theme's
 *   .info.yml file.
 *
 * @see template_preprocess_region()
 */
#}
{% if content %}
  <aside{{ attributes.addClass('col-lg-2 px-0 border border-secondary position-fixed bottom-0') }}
    id="chat" role="complementary" tabindex="-1"
    style="background-color: #F4EDFB; height: calc(100vh - 70px); right: 0">
    {{ content }}
  </aside>
{% endif %}
